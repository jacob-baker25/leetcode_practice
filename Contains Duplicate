"""
Problem: Contains Duplicate
Difficulty: Easy
Link: https://neetcode.io/problems/duplicate-integer?list=blind75

Goal:
- Given an integer array nums, return true if any value appears more than once in the array, otherwise return false.

Approach: 
- created a set that represents each integer present in the array
- checks if each value in nums is in the set
- If true, returns true and if not, adds that integer to the set
- Returns false if there are no duplicates

- Time complexity: O(n)
- Space complexity: O(n)

"""

class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        num_set = set()
        for num in nums:
            if num in num_set:
                return True
            num_set.add(num)
        return False
